// import { placeShip } from './gameboard'
// import { ship } from './ship';

// import { placeShip } from './gameboard';
// import { Ship } from './ship';
// import { receiveAttack } from './gameboard';

//Selection on the gameboard for both player and CPU
test('player taking a turn', () => {
    expect(playerAttempt()).toEqual(/*'X' placed on the grid*/);
})

test('CPU taking a turn', () => {
    expect(playerAttempt()).toEqual(/*'X' placed on the grid*/);
})